import { html } from 'lit'

export default {
  title: 'Scaffold/Example',
  render: () =>
    html`<p>Scaffold ready — implement <code>&lt;note-board&gt;</code> in <code>src/note-board/</code> and
    replace this example story.</p>`,
}
